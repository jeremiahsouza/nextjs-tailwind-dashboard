import Loading from "./loading";

const LoadingScreen = () => (
  <div className="tw-z-50 tw-fixed tw-top-0 tw-left-0 tw-w-screen tw-h-screen tw-bg-white tw-flex tw-justify-center tw-items-center">
    <Loading />
  </div>
);

export default LoadingScreen;
